{% extends 'base.html.twig' %}

{% block title %}Modifier la citation{% endblock %}

{% block body %}
    <h1>Modification de la citation</h1>

    <a href="{{ path('app_quote') }}">← Retour à la liste</a>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Enregistrer</button>
    {{ form_end(form) }}
{% endblock %}
